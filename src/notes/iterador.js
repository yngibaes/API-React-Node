/* import React from 'react'
import Clase from './comClase'

const Iterador = ({anita}) => {
    <div>
        {
            anita.map((item, index) => {
                return (
                    <Clase
                    key={index} 
                    name={item.name} 
                    img={item.img} 
                    descri={item.descri}/>
                )
            })
        }
    </div>
}

export default Iterador */